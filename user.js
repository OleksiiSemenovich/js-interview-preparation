export const count = 1;

export const getUserAge = () => {
  // возвращает возраст человека
  return 100;
};

const getUserName = () => {
  // возвращает имя человека
};

export default {
  name: "Oleksii"
};